@{
    Layout = null;
}
@model Studentski_hotel.Models.Recepcija.PrikazStudenataVM
<table class="table table-striped">
    <thead>
        <tr>
            <th>Ime i prezime</th>

            <th>Broj sobe</th>
            <th>Broj kartice</th>

            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model.Studenti)
    {
        <tr>
            <td>@item.Ime @item.Prezime</td>
            @if (item.Uselio)
            {


                <td>@item.Soba</td>


                <td>@item.BrojKartice</td>
            }
            else
            {
                <td>/</td>
                <td>/</td>
            }
            <td><button class="btn btn-success" onclick="window.location='/Recepcija/DetaljiPrikazStudenata?StudentID='+@item.ID">Pregled</button></td>
        </tr>
    }


</table>

@*<cs-pager cs-paging-pagenumber="(int)Model.PageNumber"
          cs-paging-totalitems="(int)Model.TotalItems"
          cs-paging-pagesize="Model.PageSize"
          cs-pagenumber-param="pagenumber"
          asp-controller="Recepcija"
          asp-action="PrikazZahtjeva">

</cs-pager>*@